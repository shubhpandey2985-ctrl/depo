# Backend

This directory is reserved for the DeepTech API and persistence layer.

Suggested future structure:

- `src/` — API routes, services, controllers, and data access
- `tests/` — backend tests
- `.env.example` — documented environment variables

The current DeepTech application is a frontend-only prototype that uses local state and mock data.
